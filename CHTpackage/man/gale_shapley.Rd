% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GaleShapley.R
\name{gale_shapley}
\alias{gale_shapley}
\title{Gale-Shapley Stable Matching Algorithm}
\usage{
gale_shapley(men_prefs, women_prefs)
}
\arguments{
\item{men_prefs}{A list of men's preferences (each element is a vector of women names).}

\item{women_prefs}{A list of women's preferences (each element is a vector of men names).}
}
\value{
A data.frame with columns "Man" and "Woman" representing the stable matches.
}
\description{
Implements the Gale-Shapley algorithm for stable matching.
}
\examples{
men_prefs <- list(
  A = c("Z", "X", "Y"),
  B = c("Y", "X", "Z"),
  C = c("X", "Z", "Y")
)
women_prefs <- list(
  X = c("B", "A", "C"),
  Y = c("A", "B", "C"),
  Z = c("A", "C", "B")
)
gale_shapley(men_prefs, women_prefs)
}
