---
title: "Project: Stable Matchmaking Algorithms"
author: "CAPIEU Valentin, HUCORNE Thomas & TOURNIER Valentin"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    toc_float: true
  pdf_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)
```

# Installing and Loading the package
```{r}
if (!requireNamespace("CHTpackage", quietly = TRUE)) {
if (!requireNamespace("remotes", quietly = TRUE)) {
install.packages("remotes")
}
remotes::install_github("ThomasHucorne/MatchmakingProject")
}

library(CHTpackage)

```

# Problem description
Implémente plusieurs algorithmes pour résoudre le problème de matchmaking stable entre deux ensembles (hommes et femmes). Comprend une version naïve de l’algorithme de Gale-Shapley, une variante en choissisant les meilleurs d'abord (en prenant en compte les premiers choix des hommes comme base) basée sur une liste/ vectuer. Enfin un algorithme appliqué au domaine de la santé, avec ainsi des contraintes bien précises dedans.

# Defining Preferences
```{r}
# Example preference lists

men_prefs <- list(
A = c("X", "Y", "Z"),
B = c("Y", "X", "Z"),
C = c("Y", "Z", "X")
)

women_prefs <- list(
X = c("A", "B", "C"),
Y = c("B", "A", "C"),
Z = c("A", "B", "C")
)

```

# Algorithm 1 - Naive Gale-Shapley
```{r}
result_gs <- gale_shapley(men_prefs, women_prefs)
result_gs

```

# Algorithm 2 - Gale-Shapley with Bucket
```{r}
result_bucket <- best_gs_bucket(men_prefs, women_prefs)
result_bucket

```

# Algorithm 3 - Hopcroft–Karp (Constraints in health-related setting)
Pour lancer cet algorithme, il faut lancer le Rmd de son nom, qui est dans CHTpackage/R.

# Complexity Comparison
Pour lancer la comparaison de complexité, il faut lancer Complexity_reports.Rmd.

